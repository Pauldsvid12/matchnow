import React from 'react';
import { View, StyleSheet, Text, Button } from 'react-native';
import { CameraView } from 'expo-camera'; // O legacy
import { useCameraLogic } from '@/lib/modules/camera/useCameraLogic'; // Lógica desde LIB
import { Camera } from 'lucide-react-native'; // O el icono que uses
